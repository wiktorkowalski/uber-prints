# Production Environment Configuration
# For production builds (Docker, deployment)

# API Configuration
# Empty value means use relative URLs (same-origin requests)
# This is correct for production where frontend and backend are served from the same origin
VITE_API_BASE_URL=

# Discord OAuth Configuration
# Not needed in production - these are handled server-side
# VITE_DISCORD_CLIENT_ID=
# VITE_DISCORD_REDIRECT_URI=
