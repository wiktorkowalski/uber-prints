# API Configuration
# Leave empty to use Vite proxy (recommended for development)
# The Vite proxy is configured in vite.config.ts to forward /api requests to the backend
# VITE_API_BASE_URL=

# Discord OAuth Configuration
# These are only needed if implementing client-side OAuth flow (currently not used)
# VITE_DISCORD_CLIENT_ID=your-discord-client-id
# VITE_DISCORD_REDIRECT_URI=http://localhost:5173/auth/callback
