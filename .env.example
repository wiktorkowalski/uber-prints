# Environment Configuration for UberPrints
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control - it contains sensitive secrets

# IMPORTANT: Environment variable names use double underscores (__) to map to nested config
# For example: Discord__ClientId maps to Discord:ClientId in appsettings.json

# PostgreSQL Database
POSTGRES_PASSWORD=your-strong-password-here

# JWT Configuration (must be at least 32 characters)
# Maps to Jwt:SecretKey in appsettings.json
Jwt__SecretKey=your-jwt-secret-key-minimum-32-characters-long-change-this

# Discord OAuth Credentials
# Get these from https://discord.com/developers/applications
# Maps to Discord:ClientId and Discord:ClientSecret in appsettings.json
Discord__ClientId=your-discord-client-id
Discord__ClientSecret=your-discord-client-secret

# Frontend URL for CORS configuration
# Maps to Frontend:Url in appsettings.json
Frontend__Url=http://localhost:5173

# Cloudflare Tunnel Token (optional, only needed for production deployment)
# Get this from https://one.dash.cloudflare.com/ under Networks > Tunnels
CLOUDFLARE_TUNNEL_TOKEN=your-cloudflare-tunnel-token
